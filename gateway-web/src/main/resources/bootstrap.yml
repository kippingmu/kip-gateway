spring:
  application:
    name: kip-gateway
  cloud:
    client:
      hostname: localhost
      ip-address: 127.0.0.1
    inetutils:
      default-hostname: localhost
      default-ip-address: 127.0.0.1
