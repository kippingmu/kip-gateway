spring:
  application:
    name: kip-gateway

  cloud:
    nacos:
      discovery:
        server-addr: 10.42.0.125:8848
      config:
        server-addr: 10.42.0.125:8848
    sentinel:
      transport:
        dashboard: 10.42.0.125:8080

server:
  port: 8888

logging:
  level:
    root: INFO
    org.springframework.cloud.gateway: DEBUG
    xyz.kip: DEBUG
